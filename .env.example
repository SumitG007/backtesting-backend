PORT=3001
# Required in production (comma-separated allowed browser origins). Unset = deny in prod; localhost Vite only in dev.
CORS_ORIGIN=http://localhost:5173

ADMIN=admin@example.com
ADMIN_PASSWORD=your-secure-password
JWT_SECRET=change-me-min-16-characters-long
JWT_EXPIRES_IN=7d

DHAN_CLIENT_ID=
DHAN_TOKEN_REFRESH_INTERVAL_MS=72000000
DHAN_TOKEN_RENEW_ON_STARTUP=true

MONGODB_URI=
